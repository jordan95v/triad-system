[project]
name = "parking-skeleton"
version = "0.1.0"
description = "Walking skeleton for parking reservation system"
requires-python = ">=3.13"
dependencies = [
    "django>=5.1",
    "djangorestframework>=3.15",
    "django-cors-headers>=4.6",
]

[tool.uv]
dev-dependencies = []
